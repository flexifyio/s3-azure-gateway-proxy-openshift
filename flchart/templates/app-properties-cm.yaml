apiVersion: v1 
kind: ConfigMap 
metadata:
  name: {{ .Values.app.cmName }} 
  namespace: {{ .Values.namespace }} 
data:
  {{- range $key, $value := .Values.appProperties }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
